# Aggregates crime, flood, transport, and price data from external APIs by postcode

async def get_neighbourhood_data(postcode: str) -> dict:
    # TODO: call UK Police API, Environment Agency, Transport API, ONS
    return {"crime": None, "flood_risk": None, "transport": None, "price_trends": None}
