# Computes composite 0-100 score with A-D letter grade
# Inputs: YOLO detections + neighbourhood data

def compute_score(detections: list[dict], neighbourhood_data: dict) -> dict:
    # TODO: weighted scoring logic
    return {"score": 0, "grade": "?"}
