# Pulls frames from video at 1fps, filters blurry frames via Laplacian variance
# TODO: implement with OpenCV

def extract_frames(video_path: str, output_dir: str, fps: int = 1) -> list[str]:
    return []
