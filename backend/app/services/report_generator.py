# Sends structured detection data to LLM API, receives plain-English summary

async def generate_summary(score: int, detections: list[dict], neighbourhood: dict) -> str:
    # TODO: call LLM API (Claude) with structured prompt
    return ""
